.row.paper_container
  .span12
    .gist_container
      .paper
        h3 #{paper.title}
        div.info
          span.paper_author Authors: #{paper.authors}
      .gists
        - paper.gists.order('score desc').each do |gist|
          = render 'gists/gist_in_search', gist: gist